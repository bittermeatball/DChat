import { TransactionPool } from 'src/rfr-chain/core/transaction-pool';
import { Wallet } from 'src/rfr-chain/core/wallet';

export const wallet = new Wallet('Super Secret');
export const transactionPool = new TransactionPool();
